# Copy to .env and fill in. Loaded automatically by videogen CLI.

# DashScope (阿里云百炼) API Key — REQUIRED
# 获取: https://help.aliyun.com/zh/model-studio/get-api-key
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 地域: beijing | singapore (默认 beijing)
VIDEOGEN_REGION=beijing

# 视频模型阵营: happyhorse | wan (默认 happyhorse)
# 决定 t2v / i2v / r2v 落到哪个 DashScope 模型家族:
#   happyhorse → happyhorse-1.0-{t2v,i2v,r2v}
#   wan        → wan2.7-{t2v-2026-04-25,i2v-2026-04-25,r2v}
# 可被 storyboard.provider (per-episode, 由 `scene compile --provider`
# 写入) 与 `videogen render --provider` 覆盖。功能差异:
#   - HappyHorse r2v 不支持 reference_voice 与 first_frame 续帧
#     (chain 续帧时渲染层会自动降级为 i2v)
#   - HappyHorse 不支持 negative_prompt / prompt_extend
#   - HappyHorse duration 下限为 3s (Wan 为 2s)
VIDEOGEN_VIDEO_PROVIDER=happyhorse

# 默认模型档位
VIDEOGEN_DEFAULT_RESOLUTION=720P
VIDEOGEN_DEFAULT_RATIO=16:9

# 单段视频默认时长 (秒)。默认 15 = t2v/i2v/r2v 的最大值。
# 段数越少剪切越少、人物一致性越强、API 成本越低。
# 各 provider 的下限不同 (Wan 2s, HappyHorse 3s)，由 provider 在渲染前自动钳。
VIDEOGEN_DEFAULT_CLIP_DURATION=15

# 总时长“预算关卡”阈值 (秒)。导演 Skill 看到 storyboard 总时长超过此值时，
# 必须先打印预估并向用户确认，避免一次烧掉一大笔 API 费用。
VIDEOGEN_LONG_CONFIRM_S=180

# 并发提交的最大任务数
VIDEOGEN_MAX_CONCURRENCY=4

# 轮询间隔 (秒)
VIDEOGEN_POLL_INTERVAL=15

# 项目根目录（生成视频与状态落盘）
VIDEOGEN_PROJECTS_DIR=./projects

# === Per-clip review + auto-rewrite (Zone 3) ==================================
# 每个 clip 渲染完后调用 qwen3-vl-plus 打分; score < 阈值 → 自动改 prompt + 重渲。
# 评分 = 六轴均分: logic / proportion / physics / style / cast_match / dialog_attribution。
# 其中 cast_match 用 cast.json 里的角色参考图与视频里同名角色比对身份;
# dialog_attribution 检查台词归属是否被换错(A 的台词被 B 念)。
# 阈值范围 0..10, 推荐 7.0。降低=更快通过, 提高=质量更严。
VIDEOGEN_REVIEW_THRESHOLD=7.0

# 视频审片模型 (DashScope multimodal). 设为空字符串 "" 则关闭审片。
VIDEOGEN_REVIEW_MODEL=qwen3-vl-plus

# 自动 prompt 改写模型 (DashScope text). 设为空字符串 "" 则关闭自动改写,
# 重试时仅原 prompt 重渲(只对偶发模型抖动有效)。
VIDEOGEN_REWRITE_MODEL=qwen-plus

# 单个 shot 最多渲染次数(含首次)。前 N-1 轮 CLI 自动改 prompt; 第 N 轮失败
# 写 needs_director_rewrite.json 并 exit 3, 由导演 Skill 介入。
VIDEOGEN_MAX_RETRY=3

# === 旁白解说模式 (narration mode) TTS 默认值 =================================
# /episode 起手会让用户在 drama (短剧) / narration (旁白解说, "10分钟带你看完
# XX") 之间选; 选 narration 时, 旁白 shot 渲染完后会调 TTS 合成旁白音轨,
# 通过 ffmpeg 替换掉视频原音轨; mux 对齐策略 (fit=narration):
# 成片时长 ∈ [TTS时长, TTS时长+1s] — 视频更长时保留 ≤1s 无声尾画面;
# 旁白更长时最多 freeze 1s, 超出部分裁旁白 (应靠 duration / 语速对齐).
#
# 优先级 (per-shot > per-episode > env):
#   Shot.narrator_voice (导演可单条覆盖)
#   > Storyboard.narrator_voice (scene compile --narrator-voice 写入)
#   > VIDEOGEN_NARRATOR_VOICE (此处)
#
# === TTS 后端 (按模型名前缀自动路由) ===
#   cosyvoice-*  → DashScope /services/audio/tts/SpeechSynthesizer
#                  · 龙系列系统音色 (longanyang/longwan/...) 或复刻音色
#                  · 原生 rate 字段 (0.5-2.0), 不再走 ffmpeg atempo
#                  · ⚠️ 仅北京地域可用; VIDEOGEN_REGION=singapore 时会报错
#                  · 音色列表 https://help.aliyun.com/zh/model-studio/cosyvoice-voice-list
#   qwen*-tts*   → DashScope /services/aigc/multimodal-generation/generation
#                  · CamelCase 系统音色 (Cherry/Ethan/...)
#                  · 无原生 rate; 合成后 ffmpeg atempo 调速
#                  · 音色列表 https://help.aliyun.com/zh/model-studio/qwen-tts#bac280ddf5a1u
VIDEOGEN_NARRATOR_TTS_MODEL=cosyvoice-v3-flash

# 默认音色. 必须与 VIDEOGEN_NARRATOR_TTS_MODEL 选用的后端匹配:
#   cosyvoice-*  音色 e.g. longanyang (男, 默认) / longwan (女) / longxiang
#   qwen*-tts*   音色 e.g. Cherry / Ethan / Serena / Chelsie
VIDEOGEN_NARRATOR_VOICE=longanyang

# 语速 (0.5-2.0). 默认 1.2 = 语速 1.2 倍、时长约×1/1.2,
# 减轻「旁白比画面长 → 成片定格等音频」. 设为 1.0 关闭加速.
# cosyvoice 后端走原生 rate 字段; qwen-tts 后端走 ffmpeg atempo 后处理.
VIDEOGEN_NARRATOR_SPEECH_RATE=1.2

# 旁白语言提示. Auto = 自动识别; 中文为主时填 Chinese 可显著提升发音质量.
# 可选: Auto | Chinese | English | German | Italian | Portuguese |
#       Spanish | Japanese | Korean | French | Russian
# cosyvoice 后端会自动映射为 language_hints (zh/en/...); Italian/Spanish
# 不在 cosyvoice 支持列表内, 此时退化为自动识别.
VIDEOGEN_NARRATOR_LANGUAGE=Auto

# === BGM (背景音乐) ===========================================================
# BGM 文件放到 projects/<id>/bgm/ (项目共享) 或 projects/<id>/<ep>/bgm/
# (单集独用). 支持 .mp3 .wav .m4a .flac .ogg .aac. 检测到任一目录非空时,
# /episode 起手会在 GATE 0.5 询问用户:
#   1. 使用方式 (off / global / scene)
#   2. 是否禁止模型自己生成 BGM (默认开启, 防止两条音乐打架)
# 这两个选项写入 storyboard.bgm; render 时 provider 自动把"无配乐"指令
# 加到 prompt / negative_prompt 末尾; stitch 时 ffmpeg amix 把选中的曲
# 混在人声/旁白底下.
#
# 这里只是默认音量, per-episode 可在 `bgm configure --volume` 覆盖.
# VIDEOGEN_BGM_DEFAULT_VOLUME=0.25
