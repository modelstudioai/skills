# 图像生成、视频生成与 3D 生成对比

百炼平台提供了图像生成、视频生成和 3D 生成三大类多模态内容创作 API。三者在输入输出格式、调用方式、模型生态和适用场景上存在显著差异。本文从开发者技术选型的角度，系统梳理三类能力的关键区别，帮助快速定位最适合业务需求的方案。

## 核心维度对比

| 维度 | 图像生成 | 视频生成 | 3D 生成 |
|------|---------|---------|---------|
| **主要输入** | 文本提示词、参考图像 | 文本提示词、首帧/尾帧图像、参考图/视频、音频 | 文本提示词、单张/多张图片 |
| **输出格式** | PNG 图片（单张或多张） | MP4 视频（含音频时附带音轨） | GLB 格式 3D 模型（含 PBR 材质/贴图） |
| **调用模式** | 同步调用（新版模型）或异步调用（旧版模型） | 仅异步调用（创建任务 → 轮询结果） | 仅异步调用（创建任务 → 轮询结果） |
| **典型耗时** | 秒级（同步）～分钟级（异步） | 1～5 分钟 | 分钟级（建议 15 秒轮询间隔） |
| **主要模型系列** | 千问 Qwen-Image、万相 Wan、Z-Image、可灵 Kling | 万相 Wan、HappyHorse、爱诗 PixVerse、可灵 Kling、Vidu | Tripo（Tripo-H3.1、Tripo-P1.0） |
| **模型数量** | 丰富（10+ 系列，覆盖生成/编辑/电商/创意文字等） | 丰富（5+ 厂商，覆盖文生视频/图生视频/数字人等） | 较少（2 个模型） |
| **最大输出分辨率/规格** | 最高 4K（万相 2.7）；通常 2048x2048 | 最高 1080P | 最高 200 万面（Tripo-H3.1 ultra） |
| **生成数量** | 单次 1～6 张 | 单次 1 个视频 | 单次 1 个 3D 模型 |
| **API 端点** | `/api/v1/services/aigc/...`（多个子路径） | `/api/v1/services/aigc/video-generation/video-synthesis` | `/api/v1/services/aigc/video-generation/3d-generation` |
| **请求头要求** | 标准 Authorization | Authorization + `X-DashScope-Async: enable` | Authorization + `X-DashScope-Async: enable` |
| **地域支持** | 北京（部分模型支持新加坡） | 北京为主（HappyHorse 支持多地域） | 仅北京 |
| **服务开通** | 部分模型需单独开通（如可灵） | 部分模型需单独开通（如 PixVerse、可灵、Vidu） | 需在控制台搜索"Tripo"开通 |
| **task_id 有效期** | 24 小时（异步模型） | 24 小时 | 24 小时 |

## 功能覆盖度对比

| 功能维度 | 图像生成 | 视频生成 | 3D 生成 |
|---------|---------|---------|---------|
| 文本生成 | 支持（文生图） | 支持（文生视频） | 支持（文生 3D） |
| 图像驱动 | 支持（图像编辑、风格迁移） | 支持（图生视频、首尾帧生视频） | 支持（单图/多图生 3D） |
| 视频驱动 | 不支持 | 支持（视频编辑、视频换人） | 不支持 |
| 音频驱动 | 不支持 | 支持（数字人、驱动音频） | 不支持 |
| 编辑/后处理 | 丰富（局部重绘、擦除、扩图、超分、去水印等） | 支持（视频编辑、风格重绘） | 不支持 |
| 电商/行业场景 | 丰富（虚拟模特、试衣、背景生成、海报等） | 有限（数字人、舞动人像等） | 无专用场景 |
| 多语言文字渲染 | 支持（千问图像翻译） | 不适用 | 不适用 |
| PBR 材质 | 不适用 | 不适用 | 支持 |

## 计费与资源说明

| 维度 | 图像生成 | 视频生成 | 3D 生成 |
|------|---------|---------|---------|
| 计费单位 | 按张数/分辨率 | 按视频时长/分辨率 | 按 3D 任务类型 |
| 免费体验额度 | 部分模型提供（如局部重绘、虚拟模特等为纯免费体验，用完不可续） | 视模型而定 | 视模型而定 |
| 结果有效期 | 即时返回（同步）或轮询获取 | 24 小时内获取 | 下载链接有效 2 小时 |

## 适用场景建议

### 选择图像生成的场景

- 需要快速生成营销素材、产品展示图、海报封面
- 电商场景：虚拟模特、商品背景替换、AI 试衣
- 需要对已有图片进行精细编辑（局部重绘、去水印、扩图、超分辨率）
- 需要图片中的文字渲染或翻译（如多语种商品图）
- 对响应速度要求较高，希望使用同步调用

### 选择视频生成的场景

- 需要从文本或图片生成动态视频内容（广告、短视频、宣传片）
- 数字人/虚拟主播：结合人像图片和音频生成说话、唱歌、表演视频
- 需要视频编辑能力（风格转换、内容修改）
- 多镜头叙事：需要按分镜脚本自动生成连贯视频
- 需要多主体参考生成（结合人物、场景、音色等多种素材）

### 选择 3D 生成的场景

- 需要快速获取 3D 模型用于游戏、AR/VR、工业设计预览
- 从产品照片或概念描述快速生成 3D 原型
- 需要带 PBR 材质的高精度模型（最高 200 万面）
- 多视角产品展示（多图生成模式）

## 技术选型决策参考

1. **如果需要静态视觉内容**，优先选择图像生成——模型种类最多、功能最全面、支持同步调用响应最快。
2. **如果需要动态视觉内容**，选择视频生成——支持多种驱动方式（文本/图像/音频），且有数字人等垂直场景支持。
3. **如果需要可旋转、可交互的三维资产**，选择 3D 生成——目前模型选择较少但能力专注，输出标准 GLB 格式可直接用于 3D 引擎。
4. **组合使用**：实际项目中三者可串联使用。例如先用图像生成创建高质量概念图，再用图生视频将其动态化，或用图生 3D 将产品照片转为三维模型。

## 被对比主题页

- [image generation](../api/image-generation.md)
- [video generation api](../api/video-generation-api.md)
- [3d generation](../api/3d-generation.md)


