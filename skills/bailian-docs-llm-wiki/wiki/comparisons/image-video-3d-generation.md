# 图像/视频/3D 生成对比

阿里云百炼平台通过 DashScope 统一协议向开发者暴露图像生成、视频生成与 3D 模型生成三大 AIGC 能力。三类任务在输入形态、输出产物、调用协议、模型生态与适用场景上存在显著差异，但共享相同的 API Key 体系与[异步任务](../concepts/async-task.md)轮询模型。本文面向需要做技术选型的开发者，从关键维度对三类能力进行横向对比，帮助根据业务需求选择合适的 API 与模型。

## 关键维度对比

| 维度 | 图像生成 | 视频生成 API | 3D 生成 |
| --- | --- | --- | --- |
| 代表模型家族 | 千问 Qwen-Image、万相 Wan、Z-Image、可灵 Kling | 万相 Wan 2.7、HappyHorse、爱诗 PixVerse、可灵 Kling、Vidu | Tripo（H3.1 / P1.0） |
| 输入形态 | 文本 [prompt](../guides/prompt.md)；可选参考图 / 线稿 / mask / 多人图 | 文本 [prompt](../guides/prompt.md)；可选首帧图 / 首尾帧 / 参考图 / 参考视频 / 音频 | 文本 [prompt](../guides/prompt.md)；单张图；或多视角图组（前/左/后/右） |
| 输出产物 | PNG 静态图像，1–6 张（部分模型单张） | MP4 视频，时长通常 5–10s，支持自带音频 | GLB 3D 模型（PBR / 无贴图二选一） + 预览渲染图 |
| 输出分辨率 / 规格 | 512×512 至 4K（模型相关），自由分辨率 | 480P / 540P / 720P / 1080P；多种宽高比 | 最高 200 万面（Tripo-H3.1 ultra），PBR 材质可选 |
| 调用协议 | 同步（部分千问 / Z-Image）或异步；HTTP + SDK | 纯异步：`X-DashScope-Async: enable` + 轮询 `/tasks/{task_id}` | 纯异步：同上协议，复用 `/tasks/{task_id}` 轮询 |
| 主 Endpoint | `/api/v1/services/aigc/multimodal-generation/generation`（同步）<br>`/api/v1/services/aigc/image2image/*`、`/image-generation/*`（异步） | `/api/v1/services/aigc/video-generation/video-synthesis`（人物类用 `/image2video/video-synthesis`） | `/api/v1/services/aigc/video-generation/3d-generation` |
| 单次任务耗时 | 秒级～1 分钟 | 1–5 分钟，视频编辑统一模型约 5–10 分钟 | 1–5 分钟 |
| 多地域支持 | 北京 / 新加坡（API Key 不可混用） | 北京 / 新加坡 / 弗吉尼亚 / 法兰克福 | 仅北京 |
| 典型能力覆盖面 | 文生图、图像编辑、局部重绘、扩图、背景生成、虚拟模特、人物写真、创意海报、文字翻译、WordArt 等 | 文生视频、图生视频、首尾帧、参考生视频、视频编辑、多镜头叙事、数字人、动作迁移、口型替换、唱演 / 播报 / 表情包、视频风格重绘 | 文生 3D、单图生 3D、多图生 3D（四视角） |
| 计费方式 | 按 task 计费（模型 × 输出张数 × 分辨率） | 按 task 计费（模型 × 分辨率 × 时长） | 按 task 计费（模型 × 贴图质量 × 几何精度） |
| SDK 支持 | DashScope Python / Java SDK + HTTP | DashScope Python / Java SDK + HTTP | 仅 HTTP（curl 示例官方提供） |
| task_id 有效期 | [异步任务](../concepts/async-task.md) 24h | 24h | 24h |
| 输出下载链接有效期 | 视具体 API | 视具体 API | 2 小时（需及时落盘） |

## 调用协议异同

三类能力都归属 DashScope AIGC 体系，共享 API Key（`DASHSCOPE_API_KEY`）与多地域 Endpoint 约定，但调用细节存在差异：

- **图像生成**：千问 / Z-Image 等新一代模型提供**同步**接口，可一次请求直接拿到图像 Base64 或 URL；万相、可灵及专项能力（扩图、虚拟模特、海报等）走异步协议。
- **视频生成**：**全部异步**，请求头必须带 `X-DashScope-Async: enable`，通过 `task_id` 轮询结果。wan2.7 引入多镜头叙事、参考主体独立音色等新特性，协议结构与 wan2.6 不兼容，升级时注意 `input.media` 字段变化。
- **3D 生成**：**全部异步**，Endpoint 路径复用 `video-generation` 父路径下的 `3d-generation`，请求头与轮询方式同视频生成。返回结果包含多个 URL（PBR 模型、基础模型、预览图），且有效期仅 2 小时，需要在业务侧立即转存。

## 适用场景建议

### 图像生成

- **电商营销素材批量生产**：千问 Qwen-Image 2.0 Pro 的文字渲染 + 万相扩图 / 背景生成组合，适合商品主图、Banner、创意海报流水线。
- **虚拟试衣 / 模特换装**：`wanx-virtualmodel` / `outfitanyone` 专门针对服装行业。
- **IP 形象 / 写真批量输出**：`facechain` 两图训练专属形象，适合粉丝经济、企业 IP 衍生。
- **多语言文字包装**：`qwen-mt-image` 直接在图上翻译并保留排版，跨境业务首选。
- **需要低延迟同步返回**：选择千问 / Z-Image 同步端点，秒级拿到结果。

### 视频生成 API

- **短剧 / 广告片 / 多镜头叙事**：wan2.7 文生视频支持自然语言分镜描述，无需后期剪辑即可输出连贯镜头。
- **产品展示动画**：图生视频（首帧 → 动态）适合把商品图变短视频，wan2.7-i2v 与 HappyHorse-i2v 均支持。
- **数字人 / 口播 / 培训视频**：人物驱动视频分支（数字人、口型替换、唱演）专门覆盖此类场景。
- **跨语种全球投放**：视频生成覆盖北京 / 新加坡 / 弗吉尼亚 / 法兰克福四个地域，海外合规部署最友好。
- **已有视频素材二次创作**：`wanx2.1-vace-plus` 或 `wan2.7-videoedit` 支持指令式视频编辑与多图参考重绘。

### 3D 生成

- **游戏 / XR 资产快速原型**：Tripo-H3.1 ultra 模式输出最高 200 万面高精度 GLB，可直接导入 Unity / Unreal。
- **电商 3D 展示**：单张商品图 → 3D 模型，适合鞋靴、家具、手办的 Web 端 360° 预览。
- **多视角一致性建模**：四视角多图输入模式（前 / 左 / 后 / 右），适合对几何一致性要求高的工业零件 / 文物数字化。
- **无贴图需求**：关闭 `pbr` 与 `texture` 可得到轻量化基础模型，用于低算力终端渲染。

## 技术选型决策参考

开发者在做选型时，可按以下决策顺序收敛：

1. **先确定产物形态**：静态图 → 图像生成；连续动态画面 → 视频生成；可交互三维资产 → 3D 生成。
2. **再确定调用模式**：需要毫秒 / 秒级同步响应的交互场景（如实时编辑器预览）只能选图像生成的同步端点；可接受异步轮询的批量 / 离线任务三类皆可。
3. **按地域合规筛选**：面向欧洲 / 北美用户的服务优先选视频生成（法兰克福 / 弗吉尼亚）；3D 生成目前仅北京地域，跨境业务需评估延迟与数据合规。
4. **按模型生态选版本**：
   - 图像生成：追求文字渲染与语义遵循选 Qwen-Image 2.0 Pro；追求速度选 Z-Image Turbo；已有万相工作流可继续使用 wan2.7-image。
   - 视频生成：默认推荐 wan2.7 系列（能力最全 + 多镜头叙事）；需要特殊风格或已有 PixVerse / 可灵工作流可并行评估第三方模型。
   - 3D 生成：高精度选 `Tripo-H3.1` + `geometry_quality=ultra`；快速原型 / 低面数场景选 `Tripo-P1.0`。
5. **关注成本与产物生命周期**：视频生成单次任务耗时最长、计费维度最多（分辨率 × 时长）；3D 生成的下载链接仅 2 小时有效，必须配合对象存储落盘；图像生成中异步专项模型（海报 / 虚拟模特 / 写真等）通常需要额外的素材上传步骤。

## 常见陷阱

- **API Key 跨地域混用**：三类能力在多地域部署时都要求 API Key、Endpoint、模型三者属于同一地域，混用会鉴权失败。
- **协议版本错配**：视频 wan2.7 模型必须使用新版 `input.media` 结构，沿用 wan2.6 请求体会报错。
- **3D 链接过期**：GLB 模型 URL 2 小时失效，生产环境应在任务 SUCCEEDED 后立即拉取并转存到自有 OSS。
- **重复创建任务**：三类异步 API 的 `task_id` 都支持 24h 内反复轮询，不要因超时误判而重复提交，避免重复计费。
- **图像生成同步 / 异步端点混淆**：千问 / Z-Image 同步端点不返回 `task_id`，直接返回图像；万相异步端点则需要轮询，调用前看清模型对应的协议。

## 被对比主题页

- [image generation](../api/image-generation.md)
- [video generation api](../api/video-generation-api.md)
- [3d generation](../api/3d-generation.md)


