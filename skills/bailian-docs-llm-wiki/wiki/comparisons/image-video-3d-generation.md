# 图像、视频与3D内容生成能力对比

本文旨在为开发者提供百炼平台在**图像生成（Image Generation）**、**视频生成（Video Generation）** 和 **3D模型生成（3D Generation）** 三大模态内容创作能力上的系统性对比，帮助技术团队基于任务目标、输入条件、交付要求及成本约束，快速完成模型选型与架构设计。随着AIGC应用场景从静态展示向动态交互、空间沉浸演进，理解各模态在API抽象层、能力边界与工程实践中的差异，已成为构建高质量多模态应用的关键前提。

---

## 核心能力维度对比

| 维度 | 图像生成 | 视频生成 | 3D生成 |
|------|----------|----------|--------|
| **输入格式** | 文本（`prompt`），部分模型支持图生图（`image_url` 或 `b64_image`）；万相不支持图生图，Z-Image仅支持纯文本 | 文本（`prompt`）、图像（`input_image`）、音频（`audio_url`，仅人像驱动）、或多模态组合（如 MiniMax）；所有模型均需显式声明输入类型 | 文本（`prompt`）或单张图像（`input_image`，base64/URL）；不支持多图、视频帧序列或语音输入 |
| **输出格式** | Base64 编码图像（`b64_json`）或临时可下载 URL（`.jpg`/`.png`，有效期 1 小时） | 异步返回 `.mp4` 视频直链（`output.video_url`，有效期 24 小时）；无帧序列或元数据[流式输出](../concepts/streaming-output.md) | `.glb` 格式网格文件直链（`result.url`，有效期 24 小时）；含基础材质与UV，**不输出点云、SDF、OBJ/STL等格式** |
| **支持模型** | `qwen-vl-plus`, `wanxiang-v1`, `z-image-1.0`, `kling`（需指定 `type=image`）；`vidu` **已明确不推荐用于图像任务** | `wanxiang`, `kling`, `vidu`, `happyhorse`, `aishi`（PixVerse）, `portrait-animation`, `minimax`；各模型能力子集差异显著（如 `aishi` 实际不支持图生视频） | 仅 `tripo-1.0`（Tripo 系列）；暂无其他3D模型接入，不支持多版本切换 |
| **API 端点** | `POST /v1/images/generations`（同步响应） | `POST /api/v1/video-generation`（提交任务） + `GET /api/v1/video-generation/tasks/{task_id}`（轮询） | `POST /v1/models/tripo-1.0/generate`（同步提交，异步生成，响应含 `task_id` 及轮询建议） |
| **计费方式** | 按**成功生成的图片数量**计费（`n` 参数值）；单次请求最多 4 张（Z-Image 限 1 张） | 按**成功完成的视频任务数**计费；与分辨率、时长、模型无关（但白名单扩展时长可能影响配额） | 按**成功生成的3D模型任务数**计费；免费试用限 5 次/日，商用需独立开通 `tripo-1.0` 配额 |
| **典型场景** | 社媒配图、电商主图、UI占位图、风格化插画、A/B测试多版视觉稿 | 短视频广告、产品演示动画、AI主播口播、教学微课、创意MG动画 | 游戏原型资产、AR商品预览模型、工业设计草模、WebGL可视化基础网格、3D打印初稿 |
| **延迟特性** | 同步接口：平均响应时间 2–8 秒（Z-Image 最快，万相/千问中等） | 异步任务：生成耗时 10–120 秒（`happyhorse` 最短，`vidu`/`kling` 中长视频更久）；需实现轮询逻辑 | 异步生成：典型耗时 60–180 秒；超时阈值固定为 300 秒；失败不重试，需客户端主动重提 |
| **可控性与确定性** | 支持 `seed`（千问/万相/Z-Image 均有效），相同 seed + [prompt](../guides/prompt.md) + model 可复现结果；**不支持负向提示词** | 不支持 `seed`；所有模型均无确定性控制机制；`kling` 支持 `duration`，`vidu` 支持 `aspect_ratio`，但不可控帧级一致性 | **不支持 `seed`**（文档与实测均确认忽略）；无法保证同一 [prompt](../guides/prompt.md) 多次生成结果一致；无拓扑/结构约束参数 |
| **内容安全限制** | 禁止暴力、政治敏感、成人内容；[prompt](../guides/prompt.md) 长度限制严格（万相 300 字符，Z-Image 200 字符） | 同图像生成，额外禁止生成含可识别真人肖像的视频（人像驱动除外，但需用户授权） | 禁止人脸、可识别个人信息图像输入；文本禁用政治/暴力/成人描述；静默拒绝违规请求（无错误提示） |

---

## 各方案适用场景建议

### ✅ 推荐选择图像生成当：
- 需要**毫秒级反馈**与**高并发批量出图**（如电商详情页自动化生成）；
- 任务以**风格迁移、局部重绘、图文混合理解**为核心（优先选 `qwen-vl-plus`）；
- 追求**艺术表现力与画风多样性**（如国风、赛博朋克、水彩），且 prompt 以中文为主（首选 `wanxiang-v1`）；
- 对延迟极度敏感、接受固定分辨率（1024×1024）与简化功能（如无图生图）（选用 `z-image-1.0`）；
- 已有视频生成工作流，但需**单帧高质量封面图**（使用 `kling` 并显式设置 `"type": "image"`）。

### ✅ 推荐选择视频生成当：
- 目标为**5–10秒内短视频内容**，强调动作连贯性与镜头语言（`kling` 在帧一致性上表现最优）；
- 需将**现有图片动态化**（如产品图转360°旋转视频），且对渲染质量要求适中（`wanxiang`/`vidu` 支持图生视频）；
- 构建**AI数字人播报系统**，输入人像+文案/音频即可生成口型同步视频（必须用 `portrait-animation`）；
- 需融合**文本+图像+语音**三模态指令（如“按PPT截图+讲解稿+背景音乐生成教学视频”），则选用 `minimax`；
- ⚠️ 注意规避：`aishi`（PixVerse）不支持图生视频；`vidu` 单帧质量弱于其视频整体表现，**勿用于纯图像任务**。

### ✅ 推荐选择3D生成当：
- 需快速获得**可直接嵌入WebGL/Unity/Three.js 的轻量级3D网格**（`.glb` 是行业通用交付标准）；
- 输入为**清晰单物体正视图**（如家具、器皿、玩具），追求几何合理性而非物理仿真（`tripo-1.0` 对单物体重建鲁棒性较好）；
- 作为**3D内容生产流水线的第一环节**（生成基础模型 → 导入Blender细化 → 导出用于AR）；
- ⚠️ 注意规避：非单物体、含复杂遮挡/透明材质/多人像的图像；需物理碰撞体、骨骼绑定或PBR材质的工业级需求；实时交互式建模（当前无低延迟流式3D生成能力）。

---

## 开发者技术选型参考

| 选型考量 | 推荐方案 | 关键依据 |
|----------|----------|----------|
| **首次集成，验证可行性** | 从 `z-image-1.0`（图像）或 `wanxiang`（视频）起步 | 接口最简、文档最全、错误反馈明确；避免早期陷入 `tripo-1.0` 长耗时调试或 `kling` 异步轮询复杂度 |
| **需要端到端可控性** | 图像生成（`qwen-vl-plus` + `seed`） | 唯一支持确定性复现的模态，利于A/B测试、效果归因与调试闭环 |
| **构建多模态应用（文→图→视→3D）** | 统一使用 `kling`（文生图/文生视） + `tripo-1.0`（图→3D） | `kling` 在文本理解上与 `qwen` 同源，语义一致性高；其单帧输出可作为 `tripo-1.0` 的高质量输入图像 |
| **企业级内容安全合规要求极高** | 图像生成（万相/Qwen-VL） + 视频生成（`portrait-animation`） | 二者风控策略最成熟，审核日志完备；`tripo-1.0` 对输入图像的人脸检测静默拒绝机制更符合GDPR/个保法要求 |
| **成本敏感型高频调用** | 图像生成（`z-image-1.0`） > 视频生成（`happyhorse`） > 3D生成（`tripo-1.0`） | 单次调用成本排序：Z-Image（最低）＜ HappyHorse（中）＜ Tripo（最高）；且Tripo无降级选项，失败即计费 |

> **重要提醒**：所有模态均**不支持跨模型参数复用**（如万相的 `style=anime` 对千问无效，`kling` 的 `duration` 在图像API中被忽略）。请严格依据各模型文档配置参数，避免因参数误传导致静默降级或400错误。

---  
*最后更新：2024年6月 | 文档版本：v2.3*

## 被对比主题页

- [image generation](../api/image-generation.md)
- [video generation api](../api/video-generation-api.md)
- [3d generation](../api/3d-generation.md)


