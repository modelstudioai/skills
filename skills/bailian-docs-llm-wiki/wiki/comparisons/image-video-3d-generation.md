# 图像、视频与3D内容生成能力对比

为帮助开发者快速理解百炼平台在[多模态](../concepts/multi-modal.md)内容生成领域的技术边界与工程适配要点，本文系统对比图像生成（Image Generation）、视频生成（Video Generation）与3D内容生成（3D Generation）三大能力模块。对比聚焦于**实际开发可落地的关键技术指标**，涵盖输入/输出规范、模型生态、调用范式、资源约束及计费逻辑等维度，旨在支撑技术选型决策，避免因能力误判导致的集成返工或体验降级。

---

## 核心能力对比表

| 维度 | 图像生成 | 视频生成 | 3D内容生成 |
|------|----------|----------|-------------|
| **核心任务类型** | 文生图、图生图、局部重绘、风格迁移、画布扩展 | 文生视频、图生视频、人像驱动动画、[多模态](../concepts/multi-modal.md)（文+图+音）视频生成 | 文生3D、单图生3D、多图（4视角）生3D |
| **输入格式** | • `prompt`（文本，512–1024字符）<br>• 可选 `image_url` 或 `image_base64`（图生图类）<br>• 不支持本地文件上传 | • `input.prompt`（≤512字符）<br>• 可选 `input.image_url`（图生视频）<br>• 必填 `input.audio_url`（仅人像驱动 / MiniMax）<br>• 所有 URL 需公网可访问、HTTPS、≤10 MB | • 三者**互斥**：<br> ✓ `input.prompt`（≤1024字符）<br> ✓ `input.image`（单图 URL，20–6000px，≤20 MB）<br> ✓ `input.images`（4元素数组，按【前、左、后、右】顺序，空视角填 `{}`） |
| **输出格式** | • 同步返回：`url`（可下载 HTTPS 链接）或 `b64_json`（Base64 编码 PNG）<br>• 默认返回 1–4 张图（`n` 参数控制） | • **[异步任务](../concepts/asynchronous-task.md)**：返回 `id`，需轮询 `GET /v1/videos/generations/{id}`<br>• 成功后返回 `output.video_url`（MP4，720×1280 或 1080×1920，有效期 24 小时） | • **[异步任务](../concepts/asynchronous-task.md)**：返回 `task_id`，需轮询 `GET /api/v1/tasks/{task_id}`<br>• 成功后返回多个 URL：<br> ✓ `pbr_model_url`（GLB/PBR 材质模型）<br> ✓ `base_model_url`（无贴图基础网格）<br> ✓ `rendered_image_url`（渲染预览图）<br>• 所有 URL 有效期 **2 小时**，`task_id` 有效期 **24 小时** |
| **支持模型（主流）** | • 万相（WanX）：高质量文生图，多分辨率 & 风格控制<br>• 可灵（Kling）：高保真图生图，主体一致性优<br>• Z-Image：轻量实时（512×512 固定）<br>• Qwen-VL/Qwen2-VL：[多模态](../concepts/multi-modal.md)理解协同<br>• Vidu（`type=image`）：静态帧生成（不支持负向提示词）<br>• 创意工具：inpainting/outpainting/色彩化 | • 万相 / 可灵 / Vidu：文生视频 & 图生视频（8秒）<br>• HappyHorse：轻量文生视频（4秒）<br>• 爱诗：中短时长叙事视频<br>• 人像驱动：单图+语音/文本 → 口型同步人像视频（≤15秒）<br>• MiniMax：文+图+音频三模态融合生成 | • `Tripo/Tripo-H3.1`：高精度（≤200万面），支持 `geometry_quality=ultra`<br>• `Tripo/Tripo-P1.0`：快速原型（≤2万面），推理更快 |
| **API 端点** | `POST /v1/images/generations`<br>（同步响应） | `POST /v1/videos/generations`<br>→ 轮询 `GET /v1/videos/generations/{id}` | `POST https://{WorkspaceId}.cn-beijing.maas.aliyuncs.com/api/v1/services/aigc/video-generation/3d-generation`<br>→ 轮询 `GET https://{WorkspaceId}.cn-beijing.maas.aliyuncs.com/api/v1/tasks/{task_id}`<br>（**强制华北2地域 + `X-DashScope-Async: enable` 头**） |
| **计费方式** | • 按**成功生成图片张数**计费（`n` 值）<br>• 免费试用限 `wanx-v1` 和 `zimage-v1`；`kling-v2`/`vidu-image` 需单独开通权限 | • 按**成功生成视频条数**计费<br>• 无论时长（4s/8s/15s），每条独立计费<br>• 人像驱动、MiniMax 等特殊模型需额外授权 | • 按**成功生成的3D任务次数**计费<br>• 无论输出模型精度（H3.1/P1.0）或是否启用 PBR/贴图，单次任务统一计费<br>• **仅华北2（北京）地域可用**，跨地域调用失败 |
| **典型场景** | • AIGC营销图/海报生成<br>• UI设计稿辅助出图<br>• 社交媒体配图批量生产<br>• 图像编辑（擦除/扩展/重绘） | • 短视频创意脚本可视化（<8s）<br>• 电商商品动态展示（图→视频）<br>• 虚拟人播报/口播视频生成<br>• 多模态广告片（图文音合成） | • 游戏/AR/VR资产快速建模（如道具、角色部件）<br>• 工业设计概念验证（从草图/照片生成3D原型）<br>• 电商3D商品展示（单图转3D模型）<br>• 数字孪生轻量化建模 |

---

## 各方案适用场景建议

### ✅ 图像生成 —— 适合「高频、低延迟、强可控」需求  
- **推荐场景**：需要毫秒级响应、批量生成、精细控制（如尺寸、风格、局部编辑）的业务。例如：  
  - 实时生成个性化头像/壁纸（Z-Image 低延迟）；  
  - 设计师使用万相生成多风格Banner并A/B测试；  
  - 用可灵对用户上传商品图进行背景替换或细节增强。  
- **慎用场景**：需生成动态内容、时间序列表达（如动作、过渡）、或需物理空间建模的环节。

### ✅ 视频生成 —— 适合「中短时长、强表现力、需时序逻辑」需求  
- **推荐场景**：以“视觉动效”为核心价值，且时长可控（≤15秒）的场景。例如：  
  - 信息流广告自动成片（万相/可灵文生视频）；  
  - 直播带货中商品图→3秒旋转视频（图生视频）；  
  - 客服数字人根据文本/语音生成口型同步讲解视频（人像驱动）。  
- **慎用场景**：长视频（>15秒）、高精度物理仿真（如流体/布料）、需多角度自由视角交互的场景。

### ✅ 3D内容生成 —— 适合「空间建模、资产复用、下游渲染集成」需求  
- **推荐场景**：需产出可导入Unity/Unreal/Blender的标准化3D资产，且接受异步等待（分钟级）。例如：  
  - 游戏工作室将手绘概念图一键转为低多边形模型（Tripo-P1.0）；  
  - 工业客户上传4视角产品照片生成高精度3D CAD参考模型（Tripo-H3.1 + `ultra`）；  
  - 电商平台为SKU自动生成3D商品模型用于WebGL展示。  
- **慎用场景**：实时3D生成（如Web端即时渲染）、非标准视角输入（非4视角/顺序错乱）、跨地域部署（必须华北2）。

---

## 技术选型参考（面向开发者）

| 选型考量点 | 推荐方案 | 关键依据 |
|------------|----------|-----------|
| **追求最低延迟 & 高并发** | 图像生成（Z-Image / 万相） | 同步API，平均响应 <1.5s；Z-Image 支持 50+ QPS，无异步轮询开销 |
| **需严格控制输出一致性（如主体/风格/构图）** | 图像生成（可灵 / 万相） | 可灵提供 `seed` + 主体锚点控制；万相支持 `--style` 参数与长提示词（1024字符） |
| **需生成带语音/口型同步的人像视频** | 视频生成（人像驱动） | 唯一支持音频驱动+人脸口型精准对齐的模型，输入简单（1图+1音频） |
| **需多模态融合（图文音联合生成）** | 视频生成（MiniMax） | 唯一要求并融合三模态输入的模型，适用于广告脚本到成片的一站式生成 |
| **需产出可编程接入的3D资产（GLB/FBX）** | 3D生成（Tripo） | 唯一提供标准PBR材质GLB、基础网格、渲染图三合一输出的API，直接对接引擎管线 |
| **需从单张照片重建三维结构** | 3D生成（Tripo 单图模式） | 支持任意单图（非需多视角），对输入分辨率宽容（20–6000px），适合UGC场景 |
| **需跨地域部署或混合云架构** | ❌ 3D生成不适用 | 3D API 强绑定华北2地域，Workspace ID不可复用；图像/视频API无地域限制 |

> **重要提醒**：  
> - **参数兼容性陷阱**：各模块参数命名与语义不互通（如 `negative_prompt` 在图像生成中部分有效，但在可灵视频API中被完全忽略）；务必以对应模型文档为准，勿跨模块复用参数经验。  
> - **URL时效性差异大**：图像URL长期有效（存储于百炼CDN），视频URL 24小时过期，3D所有URL仅2小时有效——下游服务需做好及时下载与缓存策略。  
> - **错误处理范式不同**：图像生成多为同步4xx错误（如400截断提示词）；视频/3D均为异步失败（轮询返回 `status=failed` + `error_code`），需实现健壮的轮询重试与超时熔断逻辑。  

---  
*最后更新：2024年6月*  
*本文档基于百炼平台 v1.2.3 版本 API 行为实测整理，具体以控制台最新文档及实际请求响应为准。*

## 被对比主题页

- [image generation](../api/image-generation.md)
- [video generation api](../api/video-generation-api.md)
- [3d generation](../api/3d-generation.md)


