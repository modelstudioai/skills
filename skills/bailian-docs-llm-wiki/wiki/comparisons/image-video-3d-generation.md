# 图像、视频与3D生成能力对比

为帮助开发者快速理解百炼平台在[多模态](../concepts/multi-modal.md)生成领域的技术边界与工程适配特性，本文系统对比图像生成（Image Generation）、视频生成（Video Generation）与3D生成（3D Generation）三大核心能力。对比聚焦实际开发中高频关注的技术维度——包括输入/输出规范、模型生态、调用方式、资源约束及商业化路径，旨在提供可落地的技术选型依据，避免因能力误判导致的集成返工或体验偏差。

---

## 关键能力维度对比

| 维度 | 图像生成 | 视频生成 | 3D生成 |
|------|----------|----------|--------|
| **输入格式** | 文本（`prompt`）、可选负向提示词；部分模型支持 `image_url`/`image_base64`（仅限 `creative-tools-v1`） | 文本（`prompt`）、图像（`image_url`，用于图生视频）、音频（`audio_url`，仅人像驱动模型）；各模型输入结构差异大 | 文本（`input.text`，≤200字符）**或** 单张图像（`input.image_url`，PNG/JPEG，需正交视角）；二者不可共存 |
| **输出格式** | JPEG/PNG 图像直链（`data[0].url`）或 Base64 编码；分辨率支持多样（如 `1024x1024`, `768x1024`） | MP4 视频直链（`output.video_url`），固定 720p（1280×720），H.264 编码；无自定义宽高比选项 | GLB 格式 3D 模型直链（`output.glb_url`）；含网格+基础材质，无动画、无骨骼、无PBR贴图 |
| **支持模型** | `qwen-vl-plus`, `wanx-v1`, `zimage-v1`, `kling-v1`, `vidu-image-v1`, `creative-tools-v1`（含局部重绘等编辑能力） | `kling`, `vidu`, `happyhorse`, `wanx`, `portrait-animation`（人像驱动）, `pixverse`, `minimax` | 仅 `tripo-3d`（v1.0），无其他模型接入计划 |
| **API 端点** | `POST /v1/images/generations` | `POST /v1/videos/generations` | `POST /v1/models/tripo-3d:generate` |
| **计费方式** | 按**成功生成的图像张数**计费（1次请求最多4张）；Vidu图像模型与Vidu视频模型**独立计费单元**，配额不可互换 | 按**成功生成的视频条数**计费（1次请求=1条视频）；人像驱动、PixVerse等模型需单独开通服务并确认配额 | 按**成功生成的GLB文件个数**计费（1次请求=1个模型）；Tripo-3D为独立计费项，不与其他生成类服务共享额度 |
| **典型场景** | - 营销海报/社交媒体配图<br>- UI设计稿初稿生成<br>- AIGC艺术创作与风格实验<br>- 局部编辑（去水印、换背景、涂鸦转图） | - 短视频内容批量生产（电商、教育、资讯）<br>- 数字人口型同步动画（直播、客服）<br>- 创意运镜短视频（广告、IP衍生）<br>- 多风格艺术化视频表达 | - 游戏/AR快速原型建模（低多边形资产）<br>- 电商商品3D预览（静物类：器皿、家具、饰品）<br>- 教育可视化教具生成（几何体、生物结构）<br>- Web3D轻量内容填充（无需复杂绑定与渲染） |
| **关键限制** | - Prompt ≤512字符<br>- 单次最多4图<br>- 不支持直接上传图作为文生图输入源（图生图需用 `creative-tools-v1`） | - Prompt 长度限制不一（Vidu ≤384字符，其余 ≤512）<br>- 所有视频固定720p，无分辨率/帧率自定义<br>- 人像驱动要求人脸占比 ≥30%、分辨率 ≥512×512 | - 输入文本 ≤200字符，图像需正交视角、背景简洁<br>- 单次请求超时阈值 180 秒<br>- 不支持批量生成（每次仅1个GLB）<br>- GLB无法线/粗糙度/金属度贴图，需后处理增强 |

---

## 各方案适用场景建议

### ✅ 推荐选择**图像生成**当：
- 需要**高可控性构图与细节表现**（如产品精修、艺术插画、UI组件生成）；
- 任务具备**强编辑需求**（擦除对象、重绘局部、扩展画布、风格迁移）；
- 输出需嵌入网页/APP且对加载速度敏感（JPEG/PNG体积小、CDN分发成熟）；
- 开发团队已具备图像处理工作流（如与Photoshop、Figma插件集成）。

> ⚠️ 注意避坑：若需“保持角色一致性生成多张图”，图像生成**无原生跨图一致性机制**，需依赖种子+相同[prompt](../guides/prompt.md)+同模型，效果有限；此时应评估视频生成中“单帧抽取”是否更优。

### ✅ 推荐选择**视频生成**当：
- 核心诉求是**时间维度表达**（运动、转场、口型同步、镜头语言）；
- 场景明确为**短时长内容消费**（2–4秒信息流广告、教学微动画、数字人播报）；
- 需要**[多模态](../concepts/multi-modal.md)驱动能力**（如人像图+语音生成带唇动的视频）；
- 对**文化语义理解深度有要求**（如中文古风、方言指令、本土节庆元素），Vidu/Kling表现更鲁棒。

> ⚠️ 注意避坑：若目标为“生成10秒以上长视频”，当前所有模型均不支持；切勿尝试拼接多段4秒视频——时序断裂明显，建议转向专业视频剪辑工具+AI辅助。

### ✅ 推荐选择**3D生成**当：
- 目标是**快速获得可交互3D资产**（WebGL/Three.js/Unity导入即用）；
- 对象类型为**静态、中低复杂度、近似对称或规则几何体**（花瓶、椅子、雕塑、建筑小品）；
- 项目处于**早期验证阶段**，无需PBR材质、骨骼动画或物理模拟；
- 团队缺乏3D建模经验，但需在24小时内交付可演示的3D原型。

> ⚠️ 注意避坑：若需生成**带动作的角色模型、机械装配体、拓扑复杂的生物结构（如人体器官）或需精确UV展开的工业零件**，Tripo-3D当前无法满足；应优先考虑传统建模+AI辅助贴图工具链。

---

## 技术选型决策参考（面向开发者）

| 决策问题 | 推荐路径 | 依据说明 |
|----------|----------|----------|
| **“我要为电商详情页生成一组不同角度的商品图”** | ❌ 不选3D生成（Tripo不支持多视角输出）→ ✅ 选**图像生成** + `creative-tools-v1` 的“视角扩展”能力（若支持）或人工调度多[prompt](../guides/prompt.md)；或 ✅ 先用3D生成得GLB → 导入Blender渲染多视角图（需额外工程） | Tripo仅输出单视角GLB；图像生成虽无原生多视角，但可通过[prompt](../guides/prompt.md)精准控制（如“front view”, “isometric angle”）实现近似效果 |
| **“我想让客户上传一张照片，实时生成其数字人说话视频”** | ✅ 必选**视频生成**中的 `portrait-animation` 模型 | 图像生成无法生成动态序列；3D生成不支持人像驱动；仅视频生成API提供专用人像驱动入口与音频对齐能力 |
| **“我需要为AR应用生成一个可旋转查看的陶瓷茶壶3D模型”** | ✅ 首选**3D生成**（Tripo-3D）→ 得GLB → 直接加载至AR框架 | 图像生成仅得静态图，无法交互旋转；视频生成输出的是视频流，非可交互3D对象；Tripo输出GLB天然适配WebXR/ARKit/ARCore |
| **“我要批量生成100张不同风格的海报，每张需含品牌Logo水印”** | ✅ 选**图像生成** + `creative-tools-v1` 的“背景替换”或“局部重绘”能力，在生成后自动叠加水印（推荐服务端合成） | 视频/3D生成均不支持水印注入；图像生成API本身不提供水印参数，但`creative-tools-v1`支持基于mask的精准编辑，适合后处理流水线集成 |
| **“我的App需在500ms内响应用户‘画一只猫’的请求并展示结果”** | ✅ 选**图像生成**（Kling/WanX）→ 响应快（通常 <3s）；❌ 视频生成（≥8s）和3D生成（≥30s）均超时 | 视频生成平均耗时 12–25s；3D生成平均耗时 40–90s（尤其high分辨率）；图像生成在多数模型下首帧返回稳定在2–4s，符合前端友好预期 |

> 💡 **终极建议**：  
> - **从最小可行输出（MVP Output）反推模型**：先明确业务最终需要的是“一张图”、“一段视频”还是“一个可旋转的3D文件”，再匹配能力域；  
> - **警惕跨模态替代陷阱**：不要用视频生成“截取帧”代替图像生成（质量损失+成本翻倍），也不要用图像生成“拼接GIF”代替视频生成（无时序建模、卡顿明显）；  
> - **计费敏感型项目务必校验配额归属**：Vidu图像与Vidu视频、Tripo-3D与所有图像/视频模型，全部为**独立计费单元**，开通前请确认预算分配。  

---  
*文档更新日期：2024年6月*  
*依据百炼平台 v2.4.0 API 规范与公开文档整理*

## 被对比主题页

- [image generation](../api/image-generation.md)
- [video generation api](../api/video-generation-api.md)
- [3d generation](../api/3d-generation.md)


