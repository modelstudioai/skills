# data connection overview

数据连接是阿里云百炼平台管理外部数据源的统一入口。通过创建数据连接器，应用可以安全地访问企业数据库、文档系统和对象存储中的数据，在对话中实时查询和引用这些数据。本文概述了所有支持的连接器类型、前置条件及关键配置要点。

## 连接器类型

数据连接器按数据的存储和访问方式分为**平台托管**和**流处理**两大类，详见 [数据连接](../../raw/application-user-guide/data-connection-overview/data-connection.md)。

### 平台托管

| 连接器类型 | 数据存储方式 | 适用场景 |
|-----------|------------|---------|
| **文件** | 百炼平台或自有 OSS | 上传和管理非结构化文档（PDF、Word、Markdown 等） |
| **表格** | 百炼平台或自有 OSS | 导入和查询结构化表格数据（CSV、Excel 等） |

### 流处理

| 连接器类型 | 数据存储方式 | 适用场景 |
|-----------|------------|---------|
| **MySQL** | 数据保留在原数据库，实时访问 | 连接 MySQL 数据库，执行 SQL 查询（仅 DMS 导入方式支持） |
| **PostgreSQL** | 数据保留在原数据库，实时访问 | 连接 PostgreSQL 数据库，执行 SQL 查询（仅 DMS 导入方式支持） |
| **PolarDB-X 2.0** | 数据保留在原数据库，实时访问 | 连接阿里云 PolarDB-X 2.0 分布式数据库 |
| **语雀** | 数据保留在语雀，实时访问 | 访问语雀文档和知识库（仅公网版本） |
| **OSS** | 数据保留在 OSS，实时访问 | 访问对象存储中的文件 |

## 前置条件

创建任何数据连接器前需满足以下通用条件：

- **账号权限**：主账号或具有数据连接管理权限的 RAM 用户。
- **数据源准备**：根据目标连接器类型准备好对应的数据源。

各连接器的特定前置条件，参见 [数据连接](../../raw/application-user-guide/data-connection-overview/data-connection.md) 中的"前置条件"章节。以下列出关键要求：

| 连接器 | 关键前置条件 |
|--------|------------|
| 文件/表格 | 已准备好文件，或已创建 OSS Bucket |
| MySQL | 已有 MySQL 实例（RDS 或自建），网络可达 |
| PostgreSQL | 已有实例，且 `wal_level` 设置为 `logical` |
| PolarDB-X 2.0 | 已有阿里云 PolarDB-X 2.0 实例，仅支持私网 |
| 语雀 | 仅支持公网版本，需获取个人访问 Token |
| OSS | 已创建 Bucket，已开通向量检索服务 |

## 创建连接器关键配置

### 存储位置选择（文件/表格连接器）

- **平台存储**：文件连接器提供最大 100,000 个文件、1 TB 存储额度（限时免费）；表格连接器提供 1 TB 免费额度，超出后按量付费。
- **自有 OSS 存储**：数据存储在自己的 OSS Bucket 中。首次使用需完成授权，且目标 Bucket 需添加 `bailian-connector-access` 标签（值为 `ReadAndWrite`）。

### 数据库连接器配置差异

| 差异项 | MySQL | PostgreSQL | PolarDB-X 2.0 |
|-------|-------|-----------|---------------|
| 默认端口 | 3306 | 5432 | 自动获取 |
| 网络类型 | 公网/私网 | 公网/私网 | **仅私网** |
| 额外必填字段 | 无 | `dbName` | 无 |
| 连通性检测服务 | EventBridge | DTS | EventBridge |
| 自建数据库 | 支持 | 支持 | **不支持** |
| 特殊配置要求 | 无 | `wal_level` 设为 `logical` | 需 DTS + PolarDB-X SLR 授权 |

> **注意**：三种数据库连接器均仅在通过**从 DMS 导入数据源**方式创建时才支持执行 SQL 查询。通过**创建自定义数据源**方式添加的连接器不支持直接执行 SQL。

### OSS 连接器注意事项

- 目标 Bucket 需添加 `bailian-datahub-access` 标签（值为 `read`），注意此标签与文件/表格连接器使用的 `bailian-connector-access` 标签不同。
- **不支持**归档、冷归档或深度冷归档存储类型的 Bucket。
- 支持内容加密和私有 Bucket。
- 需要开通向量检索服务，否则调用时返回错误。

## 数据导入

### 文件导入

文件连接器支持将非结构化文档导入平台。根据 [数据连接](../../raw/application-user-guide/data-connection-overview/data-connection.md) 中的说明，导入流程要点如下：

1. 在文件连接器详情页，通过**类目**组织文件。
2. 选择**本地上传**方式导入数据。
3. 配置**解析方式**（默认设置或自定义设置），可选：
   - **电子文档解析**：不支持解析插图与图表
   - **文档智能解析**：可识别图中文本并生成摘要
   - **大模型文档解析**：支持对插图和图表内容提问
   - **Qwen VL 解析**：仅支持图片格式，可自定义 Prompt
   - **音视频解析**：语音识别 + 视频帧提取 + 剧情解析
4. 可选配置**标签**，用于 API 调用时按标签筛选文件，提高检索效率。

> **注意**：平台不支持直接导入 JSON、CSV、YAML 格式文件，需先转换为 XLSX 或 XLS 格式。

**文件导入限制**：
- 仅支持查看最近 **90 天**内导入的文件（超期文件不可查看但不会被删除）
- 导入的文件作为独立副本存储，与原始数据无关联
- 文件仅供当前业务空间使用，不会用于商业用途或对外公开

### 表格导入

表格连接器支持两种建表方式：

- **直接上传 Excel**：自动识别表头创建数据表结构
- **自定义表头**：手动配置列名（必填）、描述（选填）、类型（必填）

> **注意**：数据表结构（列名、描述、类型）一旦确定**无法修改**。上传文件的列数和列名必须与数据表结构完全一致。若字段类型设为 `image_url`，需确保链接是公开可访问的图片 URL。

## 来源文档

- [数据连接](../../raw/application-user-guide/data-connection-overview/data-connection.md)



