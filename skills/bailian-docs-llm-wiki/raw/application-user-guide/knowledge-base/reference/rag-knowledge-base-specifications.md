# 容量与限制

阿里云百炼知识库在业务空间、文件上传、切片、知识检索与问答服务、数据集及 API 调用等环节的容量上限与使用限制。

## 空间与知识库

业务空间是资源隔离的顶层容器，每个空间下可创建多个知识库。

项目

上限

说明

单账号业务空间数

30

主账号空间标记为默认

单业务空间知识库数

50

可通过工单申请提升

知识库名称长度

1–20 字

业务空间内唯一

知识库描述长度

200 字

—

单知识库文档数

100,000

大于此值建议拆分知识库

## 文件上传

知识库支持文档、表格、图片、音视频等多种文件类型，不同类型的格式和大小约束不同。

### 文档与文本

类型

格式

单文件上限

文档

.doc / .docx / .ppt / .pptx / .pdf

150 MB 或 1000 页

纯文本

.md / .txt / .html

建议 10 MB 以内

表格

.xls / .xlsx

建议 10 MB 以内、10 万行以内

**说明**PDF 文件直接提取文本；扫描件自动 OCR 识别。表格按行切片，首行作字段名。Markdown 按标题切分，HTML 自动剥离 script / style 标签。

### 图片

项目

限制

格式

.png / .jpg / .jpeg / .bmp / .gif

单图大小

≤ 20 MB

尺寸约束

最短边 > 15px，长边 < 8192px，长宽比 < 50

### 音视频

项目

限制

音频格式

.aac / .amr / .flac / .flv / .m4a / .mp3 / .mpeg / .ogg / .opus / .wav / .webm / .wma

视频格式

.mp4 / .mkv / .avi / .mov / .wmv

单文件大小

≤ 512 MB

单次导入数量

50 个（音频与视频数量合计）

### 通用上传限制

项目

上限

单次上传文件数

50

## 切片

切片是知识库检索单元，导入时配置切分策略和分段长度。

项目

范围

默认值

最大分段长度

10–6000

600（token）

切片内容长度

0–6000 字

手动新增切片时

切片标题

0–50 字

用于切片列表展示

**警告**切片方式（智能切分 / 按长度 / 按页 / 按标题 / 按正则 / 按符号）和最大分段长度在[导入数据](raw/application-user-guide/knowledge-base/data-connection-overview/documents.md)时配置，创建后不可更改。

## 知识检索与问答服务

检索服务和问答服务是知识库对外的调用入口，以下限制在创建服务时配置。

项目

范围

默认值

单服务绑定知识库数

15

—

服务名称

最多 40 字符

必填

服务描述

最多 200 字符

可选

问答提示词

最多 500 字符

可选

初步向量检索 TopK

1–100

100

初步关键词检索 TopK

1–100

100

相似度阈值

0.01–1.0

—

最大召回数量

1–20

5

标签长度

每个最多 32 字符

输入后回车确认

## 数据集

数据集用于从外部数据源导入文件到知识库。

项目

上限

说明

文件数据集存储（平台托管）

100,000 个文件 / 1 TB

限时免费

单次上传文件数

50

—

数据集名称

20 字

—

数据集描述

200 字

—

## API 限制

### 批量操作

接口

单次上限

文档

批量删除切片

10 个

[删除切片](raw/application-api-reference/rag-api/rag-api-chunks/rag-api-delete-chunk.md)

批量查询文件

20 个 ID

[查询文件详情](raw/application-api-reference/rag-api/rag-api-data-import/rag-api-list-file.md)

### 请求频率

各接口设有请求频率限制（QPS），超出时返回 HTTP `429`。完整限流规则见[限流说明](raw/application-api-reference/rag-api/rag-api-rate-limits.md)。

接口类别

QPS 上限

检索

2,000

任务状态查询

20

文档 / 文件查询

5–15

知识库管理

10

切片管理

10

连接器管理

2–5

监控数据

1

**重要**如果当前限额无法满足业务需求，请通过工单申请提升配额。
